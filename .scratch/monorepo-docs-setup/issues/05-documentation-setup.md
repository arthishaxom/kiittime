Type: task
Status: open

Write the root `README.md` (under 100 lines, 1-command startup) and structure the `docs/` directory.
Best practices to enforce:
1. Root README must include clear monorepo onboarding (`pnpm install`, Turborepo cache usage).
2. Establish and document the ADR (Architecture Decision Record) standard in `docs/adr/` (integrating with existing ADRs).
3. Create `docs/setup.md` and `docs/architecture.md` to explain the new `apps/` and `packages/` boundaries.

