Type: task
Status: open
Blocked by: 01

Create `packages/api` and move duplicated logic (`api.ts`, `announcements.ts`, `utils.ts`, `share.ts`, `storage.ts`) from the frontend apps into this shared package. Set up its `package.json` and `tsconfig.json`.
