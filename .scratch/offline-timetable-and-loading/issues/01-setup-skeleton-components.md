Status: resolved
Type: task

Install the skeleton UI components for both mobile and web using their respective CLI tools via `pnpm`.

- Mobile: Use `react-native-reusables` CLI to install the Skeleton component.
- Web: Use `shadcn-ui` CLI to install the Skeleton component.

Ensure the installation completes successfully and the components are ready to be used in the timetable views.
