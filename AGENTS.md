- Be extremely concise. Sacrifice grammar for the sake of concision.
- Always search the exact relevant and latest information/syntax/docs for what you are doping before making changes
- Package Manager: Always use `pnpm expo` for running and managing Expo tasks; never use `npx expo` or `npm`.

## Agent skills

### Issue tracker

Issues live in GitHub Issues (`gh` CLI). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: root `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.